def main() {
    struct point { x, y }
    struct point p1
    struct point points = [50]
    points[0].x = 42
    points[0].y = 69

    array = [10]
    i = 0
    while (i < 10) {
        array[i] = 0
        i = i + 1
    }
}

def main() {
    struct point { x, y }

    let struct point points = [50]
}